namespace Breadcrumbs.SpawnSystem {
    /// <summary>
    /// 스폰할 수 있는 오브젝트 타입 정의
    /// </summary>
    public enum SpawnableObjectType {
        Monster,
        Item,
        Player,
        Boss,
        NPC
    }
}